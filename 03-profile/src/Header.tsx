import { NavbarDefault } from "./navBar";

function Header() {
    return (
      <header className="min-w-full mb-4 flex flex-col items-center justify-center">
        <NavbarDefault/>
      </header>
    );
  }

export default Header;